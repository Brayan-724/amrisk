// unsafe fn memcopy(src: usize, dst: usize, size: u16) {
//   add(end, src, size);
//   
//   'word:
//     sub(diff, end, src);
//     blt(diff, 4, 'byte);
//     
//     lw(buf, 0, src);
//     sw(buf, 0, dst);
//     
//     addi(src, src, 4);
//     addi(dst, dst, 4);
//     beq(src, end, 'end);
//     j('word);
//     
//   'byte:
//     lb(buf, 0, src);
//     sb(buf, 0, dst);
//     
//     addi(src, src, 1);
//     addi(dst, dst, 1);
//     bne(src, end, 'byte);
//     
//   'end:
//  
//   ret();
// }

// SAFE VERSION
// fn memcopy(src: word, dst: word, size: half) {
//   let end = src + dst;
// 
//   while (end - src > 4) {
//     let buf = *w src;
//     *w dst = buf;
// 
//     src = src + 4;
//     dst = dst + 4;
//   }
// 
//   while (src != end) {
//     let buf = *b src;
//     *b dst = buf;
// 
//     src = src + 1;
//     dst = dst + 1;
//   }
// 
//   dst
// }

fn main() {
  let a = 1;
  let b = 2;

  let c = add(a, b);

  let d = add(c, c)

  // print_int(a)
  // let a = 1;
  // let b = 2;

  // add(x, y)
}

fn add(a: int, b: int) { 
  a + b
}

// vim: set ft=rust
